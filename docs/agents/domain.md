# Domain Docs

This repository uses a single domain context.

## Before working

Read:

- `CONTEXT.md` for the project vocabulary.
- Relevant accepted decisions under `docs/adr/`.
- `docs/specs/2026-08-08-hk-mpf-comparison-v1-implementation-spec.md` for first-version behaviour and acceptance requirements.

Use the glossary terms in issue titles, tests and implementation. Avoid synonyms explicitly rejected by `CONTEXT.md`. Surface any conflict with an accepted ADR instead of silently overriding it.
